# Local Node.js Runtime

This folder contains the local Node.js executable used by `config-indexer.cmd` and `config-indexer.ps1`.

- Runtime: Node.js `v18.12.1`
- Platform: Windows x64
- File: `node.exe`

The indexer uses only built-in Node.js modules, so `npm install` is not required for normal use.
